import React from 'react';
import './ClientService.css';

const ClientService = () => {
    return (
        <div>
            <h1>this is client service</h1>
        </div>
    );
};

export default ClientService;